{
  "actorOutputSchemaVersion": 1,
  "title": "Google Maps Company Scraper Output",
  "properties": {
    "companyDomain": {
      "type": "string",
      "title": "Company Domain",
      "template": "{{companyDomain}}"
    },
    "companyIndustry": {
      "type": "string",
      "title": "Company Industry",
      "template": "{{companyIndustry}}"
    },
    "companyName": {
      "type": "string",
      "title": "Company Name",
      "template": "{{companyName}}"
    },
    "contactNumber": {
      "type": "string",
      "title": "Contact Number",
      "template": "{{contactNumber}}"
    },
    "emailId": {
      "type": "string",
      "title": "Email ID",
      "template": "{{emailId}}"
    },
    "exactAddress": {
      "type": "string",
      "title": "Exact Address",
      "template": "{{exactAddress}}"
    },
    "googleMapsLink": {
      "type": "string",
      "title": "Google Maps Link",
      "template": "{{googleMapsLink}}"
    },
    "locationRegion": {
      "type": "string",
      "title": "Location Region",
      "template": "{{locationRegion}}"
    },
    "reviewCount": {
      "type": "string",
      "title": "Review Count",
      "template": "{{reviewCount}}"
    },
    "servicesOffered": {
      "type": "string",
      "title": "Services Offered",
      "template": "{{servicesOffered}}"
    },
    "starRating": {
      "type": "string",
      "title": "Star Rating",
      "template": "{{starRating}}"
    },
    "totalCompaniesFound": {
      "type": "string",
      "title": "Total Companies Found",
      "template": "{{totalCompaniesFound}}"
    }
  }
}
